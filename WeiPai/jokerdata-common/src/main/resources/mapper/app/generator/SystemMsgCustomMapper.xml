<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jokerdata.mapper.app.generator.SystemMsgCustomMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.jokerdata.entity.app.generator.SystemMsg">
        <id column="notice_id" property="notice_id"/>
        <result column="notice_type" property="notice_type"/>
        <result column="notice_content" property="notice_content"/>
        <result column="is_new" property="is_new"/>
        <result column="author_name" property="author_name"/>
        <result column="add_time" property="add_time"/>
        <result column="type" property="type"/>
        <result column="data" property="data"/>
        <result column="is_all" property="is_all"/>
        <result column="is_base64" property="is_base64"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        notice_id, notice_type, notice_content, is_new, author_name, add_time, type, data, is_all, is_base64
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">

        select
        <include refid="Base_Column_List"/>
        from g_system_msg
        where del = 0
        <if test=" param.search1 != ''">
            and g_system_msg.name LIKE CONCAT('%',#{param.search1},'%')
        </if>

    </select>
</mapper>
